"""
Режим «Дома» — консистентность здания между кадрами.

Что говорят открытые гайды (обобщение, не гарантия для FastGen/Imagen):
- Img2img / image-to-image: низкая «сила» (denoising strength ~0.15–0.35 в экосистеме SD)
  лучше сохраняет силуэт и композицию; выше — модель перерисовывает сцену.
  См. обзоры по strength/denoise (напр. z-image.ai, wavespeed.ai — посты про img2img).
- Видео по референсу: коммерческие «consistent video» и исследования (RefDrop, MAGREF и др.)
  явно условятся на признаках референса; веб-скрейпер FastGen этим не управляет.
- Максимальная геометрия: ControlNet (depth/canny/lineart) в ComfyUI / API — отдельный пайплайн.

Что сделано в проекте:
- Промпты STRUCTURE_ID / house_blueprint, короткие style-hints, цепочки от скринов.
- FASTGEN_IMG2IMG_STRENGTH в .env — скрейпер пытается выставить первый/подходящий
  input[type=range] на вкладке Image после загрузки референса (эвристика).

Рекомендация: добавить в .env, подобрать по визуалу на fast-gen.ai:
  FASTGEN_IMG2IMG_STRENGTH=0.22
(или 22 если слайдер 0–100 — см. config.py: значения >1 трактуются как проценты.)
"""
