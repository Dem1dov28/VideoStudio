# Опционально: свой .env здесь, если не хочешь трогать корневой .env.
# По умолчанию Vite читает VITE_* из корня репозитория (родительская папка — см. vite.config envDir).

# VITE_API_URL=http://localhost:8000
